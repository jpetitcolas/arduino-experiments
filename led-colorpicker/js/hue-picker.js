var HuePicker = function(canvas) {
    this.canvas = canvas;
    this.context = canvas.getContext('2d');

    this.init();
};

HuePicker.prototype.init = function() {
    for (var hue = 0 ; hue < 240 ; hue++) {

    }
};
